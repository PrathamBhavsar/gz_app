# AI CONTEXT INDEX — ROOT
> **MANDATORY**: Read this file first before generating, modifying, or reviewing any code. Deviation is a critical failure.

## 🚨 THE ANTI-GREP PRIME DIRECTIVE 🚨
You are strictly forbidden from using workspace search, grep, or recursive file-finding tools as your first step. 
When asked to investigate a bug, update a feature, or add new code, you MUST follow this exact path resolution protocol:
1. Identify the domain of the user's request (e.g., "Auth", "Settings").
2. Check `brain/code_map.md` for the exact file structure.
3. Read the corresponding registry file: `brain/features/.registry/<feature>.md`.
4. Directly read the specific `.dart` files listed in the registry.
Search tools are a last resort, permitted ONLY if a file is explicitly missing from the registry and map.

## PHASE 0 — MANDATORY CONTEXT LOAD SEQUENCE
Before starting any task, load these files in order:
1. `brain/.ai_index.md` (this file)
2. `brain/code_map.md`
3. `brain/rules/state_management.md`
4. `brain/rules/layout_engine.md`
5. `brain/rules/data_layer.md`
6. `brain/features/feature_spec.md`

## TECH STACK — LOCKED
| Concern | Package | Notes |
|---|---|---|
| State | `flutter_riverpod` + `riverpod_annotation` | `@riverpod` generator only. Run `build_runner`. |
| HTTP | `dio` | Repository classes only. Never called from UI. |
| Models | `freezed` + `json_serializable` | Immutable + serializable. |
| Routing | `go_router` | Route constants in AppRoutes. |
| UI/Sizing | `flutter_screenutil` | `.r` `.sp` `.w` `.h` on ALL sizes. No raw numbers. |
