# AI BRAIN LINK
The absolute source of truth for this project is located at `brain/`.

**DO NOT WRITE, EDIT, OR DELETE CODE WITHOUT READING `brain/.ai_index.md` FIRST.**

## Strict Rules:
1. **NO GREPPING:** Use `brain/code_map.md` to find files.
2. **STATE SYNC:** If you create/delete a file, you MUST update `brain/code_map.md` and the feature registry immediately.
3. **GENERATOR:** Always run `dart run build_runner build -d` after modifying state or models.
